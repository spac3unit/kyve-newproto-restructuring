syntax = "proto3";

package kyve.delegation.v1beta1;

import "gogoproto/gogo.proto";

option go_package = "github.com/KYVENetwork/chain/x/delegation/types";


// Delegator ...
message Delegator {
  // staker ...
  string staker = 1;
  // delegator ...
  string delegator = 2;
  // k_index ...
  uint64 k_index = 3;
  // delegation_amount ...
  uint64 initial_amount = 4;
}

// DelegationEntries ...
message DelegationEntries {
  // staker ...
  string staker = 1;
  // k_index ...
  uint64 k_index = 2;
  // balance ...
  string value = 3;
  // value1
  string valueNew = 4 [(gogoproto.customtype) = "github.com/cosmos/cosmos-sdk/types.Dec", (gogoproto.nullable) = false];
}

// DelegationPoolData ...
message DelegationData {
  // Primary keys

  // staker ...
  string staker = 1;

  // F1Distribution

  // current_rewards ...
  uint64 current_rewards = 2;
  // total_delegation ...
  uint64 total_delegation = 3;
  // latest_index_k ...
  uint64 latest_index_k = 4;

  // Stats

  // delegator_count ...
  uint64 delegator_count = 5;
  // latest_index_was_undelegation ...
  bool latest_index_was_undelegation = 6;
}

// DelegationSlash ...
message DelegationSlash {
  // staker ...
  string staker = 1;
  // k_index ...
  uint64 k_index = 2;
  // fraction ...
  string fraction = 3 [(gogoproto.customtype) = "github.com/cosmos/cosmos-sdk/types.Dec", (gogoproto.nullable) = false];
}

// UndelegationQueueEntry ...
message UndelegationQueueEntry {
  // index ...
  uint64 index = 1;
  // staker ...
  string staker = 2;
  // delegator ...
  string delegator = 3;
  // amount ...
  uint64 amount = 4;
  // creation_time ...
  uint64 creation_time = 5;
}

// RedelegationCooldown ...
message RedelegationCooldown {
  // low_index ...
  string address = 1;
  // high_index ...
  uint64 creation_date = 2;
}

// QueueState ...
message QueueState {
  // low_index ...
  uint64 low_index = 1;
  // high_index ...
  uint64 high_index = 2;
}
